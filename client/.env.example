# Frontend Environment Configuration
# In local development: VITE_API_URL=http://localhost:5000 (or leave empty for Vite proxy)
# In production: leave empty for same-origin (/api) or set to deployed backend URL
VITE_API_URL=
